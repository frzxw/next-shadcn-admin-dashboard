integration_branch: split/example-initiative

workers:
  - id: A
    branch: agent/a
    allowed_paths:
      - src/server/services/ai/
  - id: B
    branch: agent/b
    allowed_paths:
      - src/server/services/db/

tasks:
  - id: T-CORE-010
    title: "SPIKE — AI Provider Selection & Routing Policy"
    type: feat
    scope: core
    goal: "Research and decide AI provider routing and adapter policy"
    depends_on: []
    allowed_paths:
      - src/server/services/ai/
    validation:
      - npm run check
    commit:
      subject: "feat(core): research spike ai provider selection routing policy"

  - id: T-CORE-001
    title: "Database Schema Foundation — Workspaces, Users, Members"
    type: feat
    scope: core
    goal: "Set up ORM and foundational workspace/user/member schema"
    depends_on: []
    allowed_paths:
      - src/server/services/db/
    validation:
      - npm run check
    commit:
      subject: "feat(core): implement database schema foundation workspaces users members"
